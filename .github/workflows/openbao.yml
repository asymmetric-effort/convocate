name: OpenBao
on:
  push:
    branches: [main]
    paths: ['infrastructure/secrets/**']
  workflow_dispatch:
jobs:
  verify:
    name: Verify prerequisites
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 2
    steps:
      - name: Verify Docker Images pipeline passed
        run: |
          LAST_RUN=$(curl -s \
            "https://api.github.com/repos/asymmetric-effort/convocate/actions/workflows/docker-images.yml/runs?per_page=1&status=completed" \
            | python3 -c 'import sys,json; runs=json.load(sys.stdin).get("workflow_runs",[]); print(runs[0]["conclusion"] if runs else "none")')
          echo "Docker Images last run: $LAST_RUN"
          if [ "$LAST_RUN" != "success" ]; then
            echo "::error::Last Docker Images run did not pass (status: $LAST_RUN)."
            exit 1
          fi

  deploy-secrets-a:
    name: Deploy to secrets-a
    needs: verify
    runs-on: [self-hosted, linux, x64]
    steps:
      - uses: actions/checkout@v7

      - name: Install Ansible collections
        run: ansible-galaxy collection install -r infrastructure/requirements.yml

      - name: Deploy secrets-a
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/ansible_key
          chmod 600 ~/.ssh/ansible_key
          ssh-keygen -f ~/.ssh/known_hosts -R 192.168.3.159 2>/dev/null || true
          ANSIBLE_HOST_KEY_CHECKING=False \
          ANSIBLE_PRIVATE_KEY_FILE=~/.ssh/ansible_key \
          ansible-playbook infrastructure/secrets/playbooks/deploy.yml \
            -e "ghcr_token=${{ secrets.GITHUB_TOKEN }}" -e "ghcr_user=${{ github.actor }}" \
            -i infrastructure/bootstrap/inventory/svr00.yml \
            -e "deploy_target=secrets-a"
          rm -f ~/.ssh/ansible_key

  deploy-secrets-b:
    name: Deploy to secrets-b (production)
    needs: deploy-secrets-a
    runs-on: [self-hosted, linux, x64]
    steps:
      - uses: actions/checkout@v7

      - name: Install Ansible collections
        run: ansible-galaxy collection install -r infrastructure/requirements.yml

      - name: Deploy secrets-b (non-destructive)
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/ansible_key
          chmod 600 ~/.ssh/ansible_key
          ssh-keygen -f ~/.ssh/known_hosts -R 192.168.3.159 2>/dev/null || true
          ANSIBLE_HOST_KEY_CHECKING=False \
          ANSIBLE_PRIVATE_KEY_FILE=~/.ssh/ansible_key \
          ansible-playbook infrastructure/secrets/playbooks/deploy.yml \
            -e "ghcr_token=${{ secrets.GITHUB_TOKEN }}" -e "ghcr_user=${{ github.actor }}" \
            -i infrastructure/bootstrap/inventory/svr00.yml \
            -e "deploy_target=secrets-b"
          rm -f ~/.ssh/ansible_key

  configure-entities:
    name: Configure Identity Entities
    needs: deploy-secrets-b
    runs-on: [self-hosted, linux, x64]
    steps:
      - uses: actions/checkout@v7

      - name: Install Ansible collections
        run: ansible-galaxy collection install -r infrastructure/requirements.yml

      - name: Create/update entities on both instances
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/ansible_key
          chmod 600 ~/.ssh/ansible_key
          ssh-keygen -f ~/.ssh/known_hosts -R 192.168.3.159 2>/dev/null || true
          ANSIBLE_HOST_KEY_CHECKING=False \
          ANSIBLE_PRIVATE_KEY_FILE=~/.ssh/ansible_key \
          ansible-playbook infrastructure/secrets/playbooks/entities.yml \
            -i infrastructure/bootstrap/inventory/svr00.yml
          rm -f ~/.ssh/ansible_key

  pdv-secrets-a:
    name: PDV Tests (secrets-a)
    needs: configure-entities
    runs-on: [self-hosted, linux, x64]
    env:
      OPENBAO_URL: https://192.168.3.160:443
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
    steps:
      - uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install test dependencies
        working-directory: infrastructure/secrets/test
        run: bun install

      - name: Run PDV tests against secrets-a
        working-directory: infrastructure/secrets/test
        run: bunx playwright test pdv.spec.ts

  smoke-secrets-b:
    name: Smoke Tests (secrets-b)
    needs: configure-entities
    runs-on: [self-hosted, linux, x64]
    env:
      OPENBAO_URL: https://192.168.3.161:443
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
    steps:
      - uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install test dependencies
        working-directory: infrastructure/secrets/test
        run: bun install

      - name: Run smoke tests against secrets-b
        working-directory: infrastructure/secrets/test
        run: bunx playwright test smoke.spec.ts
