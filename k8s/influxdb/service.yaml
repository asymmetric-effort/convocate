apiVersion: v1
kind: Service
metadata:
  name: influxdb
  namespace: convocate
spec:
  selector:
    app: influxdb
  ports:
    - port: 8086
      targetPort: 8086
      name: https
  type: ClusterIP
