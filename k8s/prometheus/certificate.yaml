apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: prometheus-selfsigned
  namespace: convocate
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus-tls
  namespace: convocate
spec:
  secretName: prometheus-tls
  issuerRef:
    name: prometheus-selfsigned
    kind: Issuer
  dnsNames:
    - prometheus
    - prometheus.convocate.svc
    - prometheus.convocate.svc.cluster.local
  duration: 8760h
  renewBefore: 720h
