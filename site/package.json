{
  "name": "@asymmetric-effort/convocate-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "convocate.asymmetric-effort.com — built with @asymmetric-effort/specifyjs",
  "scripts": {
    "build:docs-data": "bun scripts/build-docs-data.ts",
    "build": "bun run build:docs-data && vite build",
    "dev": "bun run build:docs-data && vite",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@asymmetric-effort/specifyjs": "file:./vendor-specifyjs-0.1.0-patched.tgz"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^20.0.0",
    "highlight.js": "^11.11.1",
    "marked": "^18.0.2",
    "typescript": "^5.6.0",
    "vite": "^6.4.2"
  }
}
