# Traefik Helm values for {{ cluster_name }}
# Ingress controller with TLS termination for Convocate services.
# Chart: traefik/traefik v41.0.2 (Traefik v3.7)

# Use upstream image for K8s infrastructure install (no GHCR pull secret needed)
# deploy-apps.yml handles app-level container updates via GHCR
image:
  registry: docker.io
  repository: traefik
  tag: v3.7

deployment:
  replicas: 1

nodeSelector:
  kubernetes.io/hostname: {{ traefik_node }}

tolerations:
  - key: node-role.kubernetes.io/control-plane
    operator: Exists
    effect: NoSchedule

service:
  type: ClusterIP

ports:
  web:
    port: 9080
    expose:
      default: false
  websecure:
    port: 8443
    hostPort: 443
    expose:
      default: true

providers:
  kubernetesCRD:
    enabled: true
    allowCrossNamespace: false
    namespaces:
      - convocate
  kubernetesIngress:
    enabled: true
    namespaces:
      - convocate

ingressRoute:
  dashboard:
    enabled: false
