---
# Provision VMs on svr00 hypervisor using virt-install.
# Each VM gets 3 disks: 100GB root (vda), 10GB /var/log (vdb), 5GB /tmp (vdc).
# Two NICs: cluster bridge + macvtap on LAN interface.

- name: Ensure libvirt is running
  ansible.builtin.service:
    name: libvirtd
    state: started
    enabled: true

- name: Define cluster virtual network
  ansible.builtin.shell:
    cmd: |
      if ! virsh net-info {{ cluster_bridge }} >/dev/null 2>&1; then
        cat > /tmp/{{ cluster_bridge }}-net.xml <<'NETXML'
      <network>
        <name>{{ cluster_bridge }}</name>
        <bridge name='{{ cluster_bridge }}' stp='on' delay='0'/>
        <ip address='{{ gateway_ip }}' netmask='255.255.255.0'>
          <dhcp>
            <range start='{{ cluster_network | regex_replace("\\.0/.*", ".100") }}' end='{{ cluster_network | regex_replace("\\.0/.*", ".250") }}'/>
          </dhcp>
        </ip>
      </network>
      NETXML
        virsh net-define /tmp/{{ cluster_bridge }}-net.xml
        rm -f /tmp/{{ cluster_bridge }}-net.xml
      fi
  changed_when: false

- name: Ensure cluster network is started and autostarted
  ansible.builtin.shell:
    cmd: |
      virsh net-start {{ cluster_bridge }} 2>/dev/null || true
      virsh net-autostart {{ cluster_bridge }} 2>/dev/null || true
  changed_when: false

- name: Restart cluster network DHCP (flush stale leases)
  ansible.builtin.shell:
    cmd: |
      virsh net-destroy {{ cluster_bridge }} 2>/dev/null || true
      sleep 2
      virsh net-start {{ cluster_bridge }}
  changed_when: false
  when: cluster_name is match("cluster-a")

- name: Check cloud image exists
  ansible.builtin.stat:
    path: "{{ cloud_image_path }}"
  register: cloud_image_stat

- name: Download Ubuntu cloud image if missing
  ansible.builtin.get_url:
    url: https://cloud-images.ubuntu.com/releases/24.04/release/ubuntu-24.04-server-cloudimg-amd64.img
    dest: "{{ cloud_image_path }}"
    mode: "0644"
  when: not cloud_image_stat.stat.exists

- name: Destroy existing cluster VMs (canary clusters get wiped)
  ansible.builtin.shell:
    cmd: |
      virsh destroy {{ item.name }} 2>/dev/null || true
      virsh undefine {{ item.name }} --remove-all-storage 2>/dev/null || true
      rm -f {{ vm_pool_path }}/{{ item.name }}-* 2>/dev/null || true
  loop: "{{ vm_nodes }}"
  when: cluster_name is match("cluster-a")

- name: Get list of existing VMs
  community.libvirt.virt:
    command: list_vms
  register: existing_vms

- name: Create root disk (100GB) from cloud image
  ansible.builtin.command:
    cmd: >-
      qemu-img create -f qcow2 -F qcow2
      -b {{ cloud_image_path }}
      {{ vm_pool_path }}/{{ item.name }}.qcow2
      100G
    creates: "{{ vm_pool_path }}/{{ item.name }}.qcow2"
  loop: "{{ vm_nodes }}"

- name: Create log disk (10GB)
  ansible.builtin.command:
    cmd: >-
      qemu-img create -f qcow2
      {{ vm_pool_path }}/{{ item.name }}-log.qcow2
      10G
    creates: "{{ vm_pool_path }}/{{ item.name }}-log.qcow2"
  loop: "{{ vm_nodes }}"

- name: Create tmp disk (5GB)
  ansible.builtin.command:
    cmd: >-
      qemu-img create -f qcow2
      {{ vm_pool_path }}/{{ item.name }}-tmp.qcow2
      5G
    creates: "{{ vm_pool_path }}/{{ item.name }}-tmp.qcow2"
  loop: "{{ vm_nodes }}"

- name: Generate cloud-init meta-data
  ansible.builtin.copy:
    content: |
      instance-id: {{ item.name }}-{{ ansible_date_time.epoch }}
      local-hostname: {{ item.name }}
    dest: "{{ vm_pool_path }}/{{ item.name }}-meta-data"
    mode: "0644"
  loop: "{{ vm_nodes }}"

- name: Generate cloud-init user-data
  ansible.builtin.copy:
    content: |
      #cloud-config
      hostname: {{ item.name }}
      manage_etc_hosts: true
      ssh_pwauth: true
      users:
        - name: {{ vm_user }}
          sudo: ALL=(ALL) NOPASSWD:ALL
          shell: /bin/bash
          lock_passwd: false
          passwd: {{ vm_password_hash | default('!') }}
          ssh_authorized_keys:
            - {{ ssh_public_key | default('ssh-ed25519 PLACEHOLDER') }}
        - name: sam-caldwell
          sudo: ALL=(ALL) NOPASSWD:ALL
          shell: /bin/bash
          lock_passwd: true
          ssh_import_id:
            - gh:sam-caldwell
      package_update: false
      package_upgrade: false
      runcmd:
        - apt-get update -qq && apt-get install -y -qq qemu-guest-agent && systemctl enable --now qemu-guest-agent || true
        - mkdir -p /home/sam-caldwell/.ssh && curl -sfL https://github.com/sam-caldwell.keys > /home/sam-caldwell/.ssh/authorized_keys && chmod 600 /home/sam-caldwell/.ssh/authorized_keys && chown -R sam-caldwell:sam-caldwell /home/sam-caldwell/.ssh || true
    dest: "{{ vm_pool_path }}/{{ item.name }}-user-data"
    mode: "0644"
  loop: "{{ vm_nodes }}"

- name: Create cloud-init ISO (no network-config — defaults to DHCP)
  ansible.builtin.shell:
    cmd: |
      TMPDIR=$(mktemp -d)
      cp {{ vm_pool_path }}/{{ item.name }}-user-data "$TMPDIR/user-data"
      cp {{ vm_pool_path }}/{{ item.name }}-meta-data "$TMPDIR/meta-data"
      genisoimage -output {{ vm_pool_path }}/{{ item.name }}-cidata.iso \
        -volid cidata -joliet -rock \
        "$TMPDIR/user-data" "$TMPDIR/meta-data"
      rm -rf "$TMPDIR"
    creates: "{{ vm_pool_path }}/{{ item.name }}-cidata.iso"
  loop: "{{ vm_nodes }}"

- name: Create VMs with dual NIC and 3-disk layout
  ansible.builtin.command:
    cmd: >-
      virt-install
      --name {{ item.name }}
      --ram {{ vm_memory_mb }}
      --vcpus {{ vm_cpus }}
      --disk path={{ vm_pool_path }}/{{ item.name }}.qcow2,format=qcow2,bus=virtio
      --disk path={{ vm_pool_path }}/{{ item.name }}-log.qcow2,format=qcow2,bus=virtio
      --disk path={{ vm_pool_path }}/{{ item.name }}-tmp.qcow2,format=qcow2,bus=virtio
      --disk path={{ vm_pool_path }}/{{ item.name }}-cidata.iso,device=cdrom
      --os-variant {{ vm_os_variant }}
      --network network={{ cluster_bridge }},model=e1000
      --graphics none
      --console pty,target_type=serial
      --noautoconsole
      --import
  when: item.name not in existing_vms.list_vms
  loop: "{{ vm_nodes }}"

- name: Wait for VMs to get DHCP addresses and SSH to be ready
  ansible.builtin.shell:
    cmd: |
      # Wait for DHCP lease via libvirt and SSH readiness
      # Get the VM's MAC address from libvirt
      MAC=$(virsh domiflist {{ item.name }} 2>/dev/null | grep -v '^$' | grep -v '^---' | grep -v 'Interface' | head -1 | awk '{print $5}')
      if [ -z "$MAC" ]; then
        echo "ERROR: Could not get MAC for {{ item.name }}"
        exit 1
      fi

      for i in $(seq 1 90); do
        # Try virsh net-dhcp-leases (queries dnsmasq lease database)
        IP=$(virsh net-dhcp-leases {{ cluster_bridge }} 2>/dev/null | grep "$MAC" | awk '{print $5}' | cut -d/ -f1)

        # Fallback: try virsh domifaddr with lease source
        if [ -z "$IP" ]; then
          IP=$(virsh domifaddr {{ item.name }} --source lease 2>/dev/null | grep -oE '([0-9]{1,3}\.){3}[0-9]{1,3}')
        fi

        # Fallback: try ARP table
        if [ -z "$IP" ]; then
          IP=$(arp -n | grep "$MAC" | awk '{print $1}')
        fi

        if [ -n "$IP" ]; then
          ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
              -o ConnectTimeout=3 -o BatchMode=yes \
              {{ vm_user }}@$IP 'test -f /var/lib/cloud/instance/boot-finished && echo ready' 2>/dev/null && {
            echo "DISCOVERED:$IP"
            exit 0
          }
        fi
        sleep 5
      done
      echo "TIMEOUT waiting for {{ item.name }}"
      exit 1
  register: vm_discovery
  loop: "{{ vm_nodes }}"

- name: Print VM assignments
  ansible.builtin.debug:
    msg: "{{ item.item.name }}: {{ item.stdout }}"
  loop: "{{ vm_discovery.results }}"

- name: Update inventory with discovered hosts (via svr00 jump host)
  ansible.builtin.add_host:
    name: "{{ item.item.name }}"
    ansible_host: "{{ item.stdout | regex_search('DISCOVERED:(.+)', '\\1') | first }}"
    ansible_user: "{{ vm_user }}"
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ProxyJump=samcaldwell@192.168.3.159"
    groups:
      - k8s_nodes
      - "{{ 'control_plane' if item.item.name in ['node-a-0','node-a-1','node-a-2','node-b-0','node-b-1','node-b-2'] else 'workers' }}"
  loop: "{{ vm_discovery.results }}"
  when: "'DISCOVERED' in item.stdout"
