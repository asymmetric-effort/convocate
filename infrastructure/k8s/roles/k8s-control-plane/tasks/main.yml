---
# Initialize the primary control plane node with kubeadm.
# Generates join tokens for other CP nodes and workers.

- name: Check if cluster is already initialized
  ansible.builtin.stat:
    path: /etc/kubernetes/admin.conf
  register: admin_conf

- name: Create audit log directory
  ansible.builtin.file:
    path: /var/log/kubernetes
    state: directory
    mode: "0750"
    owner: root
    group: root
  when: not admin_conf.stat.exists

- name: Render kubeadm configuration
  ansible.builtin.template:
    src: "{{ playbook_dir }}/../templates/kubeadm-config.yml.j2"
    dest: /tmp/kubeadm-config.yaml
    mode: "0600"
  when: not admin_conf.stat.exists

- name: Initialize Kubernetes cluster
  ansible.builtin.command:
    cmd: >-
      kubeadm init
      --config /tmp/kubeadm-config.yaml
      --upload-certs
      --skip-phases=addon/kube-proxy
  register: kubeadm_init
  when: not admin_conf.stat.exists

- name: Create .kube directory for root
  ansible.builtin.file:
    path: /root/.kube
    state: directory
    mode: "0700"

- name: Copy admin.conf to root .kube
  ansible.builtin.copy:
    src: /etc/kubernetes/admin.conf
    dest: /root/.kube/config
    remote_src: true
    mode: "0600"

- name: Generate bootstrap token
  ansible.builtin.command:
    cmd: kubeadm token create
  register: bootstrap_token_result
  changed_when: false

- name: Get CA certificate hash
  ansible.builtin.shell:
    cmd: >-
      openssl x509 -pubkey -in /etc/kubernetes/pki/ca.crt |
      openssl rsa -pubin -outform der 2>/dev/null |
      openssl dgst -sha256 -hex | awk '{print $NF}'
  register: ca_hash_result
  changed_when: false

- name: Generate certificate key for control plane join
  ansible.builtin.command:
    cmd: kubeadm init phase upload-certs --upload-certs
  register: cert_key_result
  changed_when: false

- name: Extract certificate key
  ansible.builtin.set_fact:
    kubeadm_certificate_key: "{{ cert_key_result.stdout_lines | last }}"

- name: Set cluster join facts
  ansible.builtin.set_fact:
    kubeadm_token: "{{ bootstrap_token_result.stdout }}"
    kubeadm_ca_hash: "sha256:{{ ca_hash_result.stdout }}"
    kubeadm_api_endpoint: "{{ cluster_ip }}:6443"
    kubeadm_cert_key: "{{ kubeadm_certificate_key }}"

- name: Fetch kubeconfig to controller
  ansible.builtin.fetch:
    src: /etc/kubernetes/admin.conf
    dest: "/tmp/kubeconfig-{{ cluster_name }}"
    flat: true

- name: Clean up kubeadm config file
  ansible.builtin.file:
    path: /tmp/kubeadm-config.yaml
    state: absent
