apiVersion: v1
kind: Service
metadata:
  name: convocate-api
  namespace: convocate
spec:
  selector:
    app: convocate-api
  ports:
    - port: 8443
      targetPort: 8443
      name: api
  type: ClusterIP
