apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgresql-pv
spec:
  capacity:
    storage: {{ .Values.storage | default "5Gi" }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ .Values.hostPath | default "/mnt/data-layer/postgresql" }}
    type: DirectoryOrCreate
  storageClassName: local
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - {{ .Values.nodeAffinity | default "convocate05" }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql-data
  namespace: {{ .Values.global.namespace | default "data-layer" }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local
  volumeName: postgresql-pv
  resources:
    requests:
      storage: {{ .Values.storage | default "5Gi" }}
