apiVersion: v1
kind: Service
metadata:
  name: convocate-api
  namespace: {{ .Values.global.namespace | default "convocate" }}
  labels:
    app: convocate-api
spec:
  selector:
    app: convocate-api
  ports:
    - port: 443
      targetPort: {{ .Values.port | default 443 }}
      name: api
  type: ClusterIP
