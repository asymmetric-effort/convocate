apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: redis
  namespace: {{ .Values.global.namespace | default "data-layer" }}
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  type: ClusterIP
