apiVersion: v1
kind: Service
metadata:
  name: influxdb
  namespace: {{ .Values.global.namespace | default "o11y" }}
spec:
  selector:
    app: influxdb
  ports:
    - port: 8086
      targetPort: 8086
      name: https
  type: ClusterIP
