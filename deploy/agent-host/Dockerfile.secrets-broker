FROM gcr.io/distroless/base-debian12

ARG VERSION=dev

LABEL org.opencontainers.image.title="convocate-secrets-broker" \
      org.opencontainers.image.version="${VERSION}" \
      org.opencontainers.image.description="Convocate per-container OpenBao socket multiplexer"

COPY build/convocate-secrets-broker /usr/local/bin/convocate-secrets-broker

ENTRYPOINT ["/usr/local/bin/convocate-secrets-broker"]
