import { Component } from "@asymmetric-effort/specifyjs";

export class Agents extends Component<Record<string, never>, Record<string, never>> {
  state = {};

  render() {
    return (
      <div>
        <h1>Agents</h1>
        <p>Agent management coming soon.</p>
      </div>
    );
  }
}
